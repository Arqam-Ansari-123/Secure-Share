# Default surface. The PUBLIC build is the safe default: if a build ever runs
# without an explicit mode, it produces the client-facing bundle with no staff
# code in it, rather than accidentally shipping the internal app.
VITE_SURFACE=public
